DISCORD_TOKEN=
# Optional. Required only if your llama.cpp server is configured with --api-key.
LLAMA_CPP_API_KEY=
# Optional. Override the default ./config.json path when needed.
# PETERBOT_CONFIG_FILE=/app/config.json
